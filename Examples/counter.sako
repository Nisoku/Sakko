<counter {
  @state { count = 0 }

  text(bold xlarge): "Count: {count}"

  column(gap small): [
    button(accent) @on:click { count++ }: "+1",
    button @on:click { count-- }: "-1",
    button(danger) @on:click { count = 0 }: "Reset"
  ]
}>
